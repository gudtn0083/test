#ifndef SORT_H
#define SORT_H

#include <stddef.h>

/* 정수 배열을 오름차순으로 정렬 */
void sort_int_asc(int arr[], size_t n);

/* 실수 배열을 오름차순으로 정렬 */
void sort_double_asc(double arr[], size_t n);

#endif /* SORT_H */